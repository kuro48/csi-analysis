[package]
name = "csi-breathing-guest"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
csi-zkvm-core = { path = "../../core", default-features = false }
risc0-zkvm = { version = "3.0.5", default-features = false }
